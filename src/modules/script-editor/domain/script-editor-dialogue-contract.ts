import type {
  DialogueSide,
  RuntimeDialogueCastMember,
  RuntimeDialogueDefinition,
  RuntimeDialogueNode,
} from "../../../domain/dialogue";

export type {
  DialogueSide,
  RuntimeDialogueCastMember,
  RuntimeDialogueDefinition,
  RuntimeDialogueNode,
};
