# Review package: 83f2744..09ab59f

## Commits
09ab59f feat: add spine editor unit selector

## Files changed
 tests/spine-unit-context.test.cjs     | 18 ++++++++++++++++++
 tools/spine-node-timeline-editor.html | 14 ++++++++++++++
 2 files changed, 32 insertions(+)

## Diff
diff --git a/tests/spine-unit-context.test.cjs b/tests/spine-unit-context.test.cjs
index fe2f4ed..bc74858 100644
--- a/tests/spine-unit-context.test.cjs
+++ b/tests/spine-unit-context.test.cjs
@@ -16,15 +16,33 @@ test("Spine editor defines a unit registry for swordsman and archer", () => {
   );
 });
 
 test("Spine editor switches unit context only after a project load succeeds", () => {
   assert.match(source, /async function switchSpineUnitContext\(unitType\)/);
   assert.match(source, /const project = await loadProjectJsonFile\(config\.projectUrl\)/);
   assert.match(source, /if \(!project\) \{[\s\S]*return false;[\s\S]*\}/);
   assert.match(source, /state\.currentUnitType = unitType;/);
 });
 
+test("Spine editor exposes top-level swordsman and archer unit buttons", () => {
+  assert.match(source, /id="unitContextToolbar"/);
+  assert.match(source, /id="unitSwordsmanBtn"/);
+  assert.match(source, /id="unitArcherBtn"/);
+  assert.match(source, /currentUnitType:\s*"swordsman"/);
+});
+
+test("Spine editor binds the unit buttons to switchSpineUnitContext", () => {
+  assert.match(
+    source,
+    /el\.unitSwordsmanBtn\.addEventListener\("click", \(\) => switchSpineUnitContext\("swordsman"\)\)/,
+  );
+  assert.match(
+    source,
+    /el\.unitArcherBtn\.addEventListener\("click", \(\) => switchSpineUnitContext\("archer"\)\)/,
+  );
+});
+
 test("Spine editor gates swordsman and archer feature groups by unit context", () => {
   assert.match(source, /id="swordsmanFeatureGroup"/);
   assert.match(source, /id="archerFeatureGroup"/);
   assert.match(source, /function renderSpineUnitFeatureGroups\(\)/);
 });
diff --git a/tools/spine-node-timeline-editor.html b/tools/spine-node-timeline-editor.html
index 18e44a1..d169d8f 100644
--- a/tools/spine-node-timeline-editor.html
+++ b/tools/spine-node-timeline-editor.html
@@ -578,20 +578,24 @@
   <body>
     <div class="app">
       <header class="topbar">
         <div class="brand">
           <div class="brand-mark" aria-hidden="true"></div>
           <div>
             <h1>Spine 节点时间轴编辑器</h1>
             <span>骨骼节点 · 关键帧 · 插值播放 · JSON 工程导入导出</span>
           </div>
         </div>
+        <div id="unitContextToolbar" class="toolbar unit-toolbar">
+          <button id="unitSwordsmanBtn" class="mode-button active" type="button">剑士</button>
+          <button id="unitArcherBtn" class="mode-button" type="button">弓兵</button>
+        </div>
         <div class="toolbar">
           <div id="swordsmanFeatureGroup" class="toolbar-group" hidden></div>
           <div id="archerFeatureGroup" class="toolbar-group" hidden></div>
           <button id="newProjectBtn" type="button">新建</button>
           <button id="importBtn" type="button">导入 JSON</button>
           <button id="exportBtn" type="button">导出 JSON</button>
           <button id="sampleBtn" type="button">示例</button>
           <button id="walkSampleBtn" type="button">侧面行走</button>
           <button id="newVersionSampleBtn" type="button">新版</button>
           <button id="idleAnimationBtn" type="button">待机动画</button>
@@ -1128,20 +1132,22 @@
         const cached = cache.get(track);
         if (cached && cached.duration === duration) return cached.value;
         const value = normalizeFn(track, duration);
         cache.set(track, { duration, value });
         return value;
       }
 
       const el = {
         app: document.querySelector(".app"),
         stage: document.getElementById("stage"),
+        unitSwordsmanBtn: document.getElementById("unitSwordsmanBtn"),
+        unitArcherBtn: document.getElementById("unitArcherBtn"),
         actionList: document.getElementById("actionList"),
         actionNameInput: document.getElementById("actionNameInput"),
         newActionBtn: document.getElementById("newActionBtn"),
         saveActionBtn: document.getElementById("saveActionBtn"),
         applyIdleToActionBtn: document.getElementById("applyIdleToActionBtn"),
         deleteActionBtn: document.getElementById("deleteActionBtn"),
         nodeList: document.getElementById("nodeList"),
         pieceList: document.getElementById("pieceList"),
         addNodeBtn: document.getElementById("addNodeBtn"),
         addPieceBtn: document.getElementById("addPieceBtn"),
@@ -3561,34 +3567,40 @@
       }
 
       function setDrawAlpha(alpha) {
         ctx.globalAlpha = Math.max(0, Math.min(1, alpha * drawAlphaMultiplier));
       }
 
       function getSpineUnitConfig(unitType) {
         return SPINE_UNIT_CONFIGS[unitType] || SPINE_UNIT_CONFIGS.swordsman;
       }
 
+      function renderSpineUnitContextControls() {
+        el.unitSwordsmanBtn?.classList.toggle("active", state.currentUnitType === "swordsman");
+        el.unitArcherBtn?.classList.toggle("active", state.currentUnitType === "archer");
+      }
+
       function renderSpineUnitFeatureGroups() {
         const config = getSpineUnitConfig(state.currentUnitType);
         const activeGroups = new Set(config.featureGroups || []);
         el.swordsmanFeatureGroup.hidden = !activeGroups.has("swordsman");
         el.archerFeatureGroup.hidden = !activeGroups.has("archer");
       }
 
       function renderAll() {
         resetRuntimeCaches();
         if (!drag || drag.type === "key" || !drag.hasMoved) {
           poseOverrides.clear();
         }
         syncAllBowRigs();
         syncCurrentActionFromTimeline();
+        renderSpineUnitContextControls();
         renderSpineUnitFeatureGroups();
         renderActionList();
         renderNodeList();
         renderPieceList();
         renderProperties();
         renderBindingPanel();
         renderTimeline();
         renderStatus();
         renderJsonPreview();
         drawStage();
@@ -9070,20 +9082,22 @@
             renderAll();
             toast("JSON 文本已应用");
           } catch (error) {
             toast(error.message || "JSON 解析失败");
           }
         });
         el.saveJsonFileBtn.addEventListener("click", saveProjectJsonFile);
         el.sampleBtn.addEventListener("click", loadSample);
         el.walkSampleBtn.addEventListener("click", loadSideWalkSample);
         el.newVersionSampleBtn.addEventListener("click", loadNewVersionSample);
+        if (el.unitSwordsmanBtn) el.unitSwordsmanBtn.addEventListener("click", () => switchSpineUnitContext("swordsman"));
+        if (el.unitArcherBtn) el.unitArcherBtn.addEventListener("click", () => switchSpineUnitContext("archer"));
         el.replaceMaterialBtn.addEventListener("click", () => el.materialInput.click());
         el.materialInput.addEventListener("change", () => applyMaterialReplacement(el.materialInput.files[0]));
         el.idleAnimationBtn.addEventListener("click", applyIdleAnimation);
         el.bindingModeBtn.addEventListener("click", () =>
           setWorkspaceMode(isBindingMode() ? "animation" : "binding"),
         );
         el.addWeaponBoneBtn.addEventListener("click", addWeaponBone);
         el.addRootBoneBtn.addEventListener("click", addRootBone);
         el.applyWeaponImageBtn.addEventListener("click", applyWeaponImage);
         el.addBoneFromNodeBtn.addEventListener("click", () => setBindingBoneTool("from-node"));
